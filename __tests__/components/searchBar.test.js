describe('SearchBar', () => {
    it('calls the api with the search term', () => {
        // type in search box
        // click button
        // expect api to have been called with search term
    })
})